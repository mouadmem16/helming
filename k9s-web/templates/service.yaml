apiVersion: v1
kind: Service
metadata:
  name: {{ include "k9s-web.fullname" . }}
  labels:
    {{- include "k9s-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k9s-web.selectorLabels" . | nindent 4 }}
